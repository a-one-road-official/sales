name: Retired Python continuous outbound

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "Customer-facing email sending has moved to ChatGPT Outbound A/B using the connected Gmail tool."
          echo "Python/GitHub customer email sending is intentionally disabled."
          exit 1
