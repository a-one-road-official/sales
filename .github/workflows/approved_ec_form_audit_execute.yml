name: Approved EC form audit execute (disabled - list only)

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - name: Confirm list-only mode
        run: |
          echo "List-only mode is enforced."
          echo "Customer-facing sends, forms, invitations, and CRM notifications are disabled."
          echo "This workflow intentionally performs no external action."
