name: Retired duplicate continuous SSOT outbound

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "Retired duplicate workflow. Canonical continuous sender is .github/workflows/continuous_outbound.yml"
          exit 1
