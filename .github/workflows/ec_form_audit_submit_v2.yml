name: EC form audit submit v2 (disabled - BPO only)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo "EC outbound is disabled; BPO is the only approved outbound lane."
