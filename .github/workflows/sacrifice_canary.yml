name: EC sacrifice canary (disabled - A1 list only)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: A1 fail-closed interlock
        run: |
          echo "A1 interlock: EC sacrifice execution is disabled."
          echo "No external email, form submission, invitation, or CRM notification is executed."
