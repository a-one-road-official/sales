name: EC sacrifice canary (disabled - list only)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Confirm list-only mode
        run: |
          echo "List-only mode is enforced."
          echo "Customer-facing sends, forms, and invitations are disabled."
          echo "This workflow intentionally performs no external action."
